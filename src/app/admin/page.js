import AdminLeads from "./AdminLeads";

export const metadata = {
  title: "Заявки | Типми",
  robots: {
    index: false,
    follow: false,
  },
};

export default function AdminPage() {
  return <AdminLeads />;
}
